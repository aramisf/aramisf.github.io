function _1oCtrl ($scope) { // $scope eh tudo o que estiver dentro da tag que
                            // contem o 'ng-controller=1oCtrl'

    $scope.dados = {        // note que isto eh uma hash
            msg: "Mensagem"
        };
            
}
